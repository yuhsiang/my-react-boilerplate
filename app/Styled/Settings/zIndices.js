
/**
 * Everything relative to the page element
 */
const Z_PAGE = 100;
const Z_PAGE_MENU = Z_PAGE + 1;

/**
 * Modal and floating element
 */
const Z_INDEX_BASE_FLOAT = 1000;
const Z_FIXED_OVERLAY = Z_INDEX_BASE_FLOAT + 1;
const Z_FIXED_MODAL = Z_INDEX_BASE_FLOAT + 2;

export default {
  Z_PAGE,
  Z_PAGE_MENU,
  Z_FIXED_OVERLAY,
  Z_FIXED_MODAL,
};
