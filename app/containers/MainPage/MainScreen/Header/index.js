import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

const  HeaderScreen = () => (
  <Header>
    <h1>
      Beauty Boilerplate
    </h1>
  </Header>
);
export default HeaderScreen;
